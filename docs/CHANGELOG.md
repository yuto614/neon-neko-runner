# CHANGELOG

NEON NEKO RUNNER のバージョン履歴です。

## v1.6「Challenge Missions Update」

* 実績（Achievements）とは別のミッションシステムを追加（Fish Collector / Hard Challenger / Rare Hunter / Survivor の4種類）
* ミッションは配列(`MISSIONS`)でデータ管理し、`id` / `title` / `description` / `type` / `target` / `rewardType` / `rewardValue` / `completed` を持つ構造に（今後の追加は配列への要素追加と`type`分岐の追加のみで対応可能）
* ミッション画面(MISSIONS)を追加し、タイトル画面からCキーで遷移可能に。↑↓でミッション切替、タイトル・説明・進捗・達成状況・報酬を表示
* Fish Collector: 累計魚100匹でPink Catを解放 / Rare Hunter: 累計レア魚20匹でGolden Catを解放 / Survivor: Shieldでゲームオーバーを5回防いでShadow Catを解放
* Hard Challenger: Hardモードでスコア5000達成で「+500 Bonus Score」を表示（実際のスコアには加算されない表示のみの報酬）
* ミッション報酬のスキンは未解放の場合のみ解放し、既存のスキン解放通知（NEW SKIN!）も連動する。スキン側の条件で先に解放済みの場合は二重解放しない
* 達成時は画面右上に「MISSION COMPLETE!」を表示（実績解除・スキン解放と同じ通知キュー方式を流用）
* 進捗判定はゲームオーバー時に実行。Hardモードの自己最高スコア・Shieldでのゲームオーバー回避回数・達成済みミッションIDを`cat-game-missions`に保存し、起動時に読み込む
* 既存の実績システムには影響しない。レベルシステム・ゲームバランス・既存操作に変更なし

## v1.5「Game Modes Update」

* ゲーム開始前にプレイスタイルを選べるモード選択システムを追加（Classic / Hard の2種類）
* モードは配列(`GAME_MODES`)でデータ管理し、`id` / `name` / `description` / `obstacleSpeedMultiplier` / `fishSpeedMultiplier` / `itemSpeedMultiplier` / `scoreMultiplier` を持つ構造に（今後の追加は配列への要素追加のみで対応可能）
* モード選択画面(MODE SELECT)を追加し、タイトル画面からMキーで遷移可能に。↑↓でモード切替、SPACEで選択
* Classic: Version1.4までと完全に同じ挙動（全倍率1.0倍）
* Hard: 障害物速度+25% / 魚速度+15% / アイテム速度+15% / スコア倍率1.5倍を適用
* レベル算出・障害物速度の自動加速カーブはスコア倍率を含まない素点(`rawScoreForLevel`)で判定し、Hardモードでもレベルアップの実時間ペースをClassicと一致させる
* タイトル画面に選択中のモード名（例: 「MODE : CLASSIC」）を表示し、操作説明に「M : MODE」を追加
* HUD左上に「MODE: CLASSIC」/「MODE: HARD」を常時表示
* 選択中のモードIDを`localStorage`に保存し、起動時に読み込み・ゲーム開始時に適用（プレイ中の変更は不可）
* 既存のジャンプ操作・難易度カーブの定数・実績/スキン/アイテムシステムに変更なし

## v1.4「Power-Up Update Part 2」

* アイテムシステムにScore Boost（ゴールド・10秒）とSlow Time（ライトブルー・6秒）を追加し、4種類に拡張
* Score Boost: 効果中はスコア加算（生存時間・ニアミス・魚収集すべて）を2倍にする `addScore` 関数を追加し、既存のスコア加算箇所を置き換え
* Slow Time: 障害物・魚・アイテムの移動速度のみ約70%に低下させる（プレイヤー速度・ジャンプ・背景スクロールは変更なし）
* アイテム取得時のポップアップに「SCORE x2!」/「SLOW TIME!」を追加（既存の魚取得演出を流用）
* Score Boost中はゴールドのリング、Slow Time中は薄い青色のオーラをプレイヤー周囲に表示（既存のリング/オーラ描画を流用）
* HUD右上にScore x2 / Slow Timeの残り秒数を追加表示（未取得時は非表示）
* `scoreBoostTimer` / `slowTimeTimer` をShield・Magnetと同様の方式で管理し、ゲーム終了時にリセット（`localStorage`には保存しない）
* アイテム出現率・抽選方式はVersion1.3から変更なし（4種類均等抽選、画面内最大1個）
* 効果音は既存の`item.wav`を再利用（追加音源なし）
* レベルシステム・ゲームバランス・既存操作に変更なし

## v1.3「Power-Up Update Part 1」

* ゲーム中に出現するアイテムシステムの基盤を追加（魚・障害物とは独立管理、画面内最大1個、一定時間ごとに抽選で出現）
* アイテムは配列(`ITEM_DEFS`)でデータ管理し、`id` / `name` / `color` / `duration` / `effect` を持つ構造に（今後の追加は配列への要素追加が中心）
* Shield（シアン・5秒）を追加。効果中は障害物への衝突を1回まで無効化し、シールドのみを消費する
* Magnet（マゼンタ・8秒）を追加。効果範囲内の魚を自動的に引き寄せて回収する
* アイテム取得時にスパークル・リング・ポップアップ（「SHIELD!」/「MAGNET!」）を表示（既存の魚取得演出を流用）
* Shield中はシアンのリング、Magnet中はマゼンタのオーラをプレイヤー周囲に表示
* HUD右上にShield / Magnetの残り秒数を表示（未取得時は非表示）
* 効果音`item.wav`を追加（音源は未配置）
* アイテム効果は`localStorage`に保存せず、ゲーム終了時にリセット
* レベルシステム・ゲームバランス・既存操作に変更なし

## v1.2

* 猫スキン・コレクション機能を追加（Cyber Cat / Pink Cat / Golden Cat / Shadow Cat の4種類）
* スキンは配列(`SKINS`)でデータ管理し、`id` / `name` / `color` / `unlockType` / `unlockValue` / `unlocked` を持つ構造に（今後の追加は配列への要素追加のみで対応可能）
* スキン選択画面(SKINS)を追加し、タイトル画面からKキーで遷移可能に。←→でスキン切替、SPACEで装備（未解放スキンは装備不可）
* ハイスコア・累計レア魚取得数・全実績解除状況に応じたスキン解放判定を、ゲームオーバー時・タイトルへ戻ったタイミングで実行
* 新規スキン解放時に画面右上へ「NEW SKIN!」通知を表示（複数同時解放にも対応するキュー方式）
* 装備中スキンのカラーをプレイヤー本体・尾・肉球・トレイルの描画に反映
* 装備中スキンID・解放済みスキン一覧を`localStorage`に保存
* タイトル画面の操作説明を「SPACE : START」「S : SETTINGS」「A : ACHIEVEMENTS」「K : SKINS」の形式に整理
* レベルシステム・ゲームバランス・難易度に変更なし

## v1.1

* BGM（タイトル / プレイ中 / ゲームオーバー）の再生構成を追加（`Audio` オブジェクトを `assets/audio/bgm/` から読み込む。音源は未配置）
* 効果音(SE)8種類（ジャンプ・ダブルジャンプ・魚取得・レア魚取得・レベルアップ・実績解除・ゲームオーバー・ボタン決定）の再生構成を追加（`assets/audio/se/` から読み込む。音源は未配置）
* 設定画面(SETTINGS)を追加し、タイトル画面からSキーで遷移可能に
* 設定画面でBGM音量・SE音量・BGM ON/OFF・SE ON/OFFを変更可能にし、`localStorage` に保存
* `assets/audio/` ディレクトリ（`bgm/` / `se/`）と必要な音源一覧を記載した README を追加
* レベルシステム・ゲームバランス・操作感に変更なし

## v1.0 正式リリース

* README にプレイ環境・GitHub Pages公開手順を追記
* キャンバスをレスポンシブ対応（`max-width: 100%` / `height: auto`）に調整
* `index.html` に `description` メタタグを追加
* ヒットストップ・演出タイマーの優先順位に関するコメントを整理し、正式リリース版として確定

## v0.95 公開準備版

* README を拡充し、操作方法・主な機能・公開URL（GitHub Pages）を記載
* ダブルジャンプを実装（最大2回ジャンプ、専用パーティクル・リング・本体グロー演出を追加）
* ジャンプ・ゲーム開始処理を `handlePrimaryAction` に整理

## v0.9 ゲームフィール強化

* ヒットストップ（魚取得・レア魚・レベルアップ・実績解除時に1フレーム全体を停止）を追加
* 画面シェイク（レア魚・レベルアップ・ゲームオーバー時）を追加
* スコアのポップ演出・ハイスコア更新時の発光演出を追加
* 魚取得時のリングエフェクトをコンボ演出と統合し、コンボ段階に応じた色・発光を調整

## v0.8 魚収集・実績

* 通常魚・レア魚の出現とスコアボーナス（+200 / +500）を追加
* 魚取得時のスパークルパーティクル・ポップアップ表示を追加
* 実績システム（First Jump / Combo Master / Fish Hunter / Rare Collector / Cyber Legend の5種）を追加
* 累計魚取得数・レア魚取得数・解除済み実績を `localStorage` に保存
* 実績解除通知・実績一覧画面を追加

## v0.7 障害物バリエーション

* 障害物を6種類（block / yarn / bone / vacuum / dog / trashcan）に拡張し、種類ごとの形状を描画
* レベルに応じた障害物の出現解放（レベル3でyarn/bone/vacuum追加、レベル5で全種類）
* 障害物ごとの速度倍率（見た目の速度差のみに使用）を追加
* ホログラム広告演出（レベル5以上で出現）を追加

## v0.6 ニアミス・コンボ

* ニアミス判定（ジャンプ中に障害物と接近した際の成立条件）を追加
* コンボシステム（連続ニアミスでボーナス増加、一定時間ニアミスがないとリセット）を追加
* コンボ段階に応じた演出（色・発光の変化）と「NICE!」「COMBO ×N」ポップアップを追加
* スコアのマイルストーン演出（1000 / 3000 / 5000 / 10000到達時）を追加
* レベルアップ時のリング演出を追加

## v0.5 猫マスコット改善

* プレイヤーを猫らしいシルエット（耳・尾・肉球・ヒゲ）で再描画
* 表情（通常・まばたき・笑顔・ジャンプ中）を状況に応じて切り替え
* 走行アニメーション（肉球の位置切り替え）・尾の揺れアニメーションを追加
* 着地時のハッピー表情・着地パーティクルを追加

## v0.4 レベルシステム

* スコアに応じたレベル算出とレベルアップ演出（「LEVEL UP!」テキスト）を追加
* レベルに応じた障害物速度の上昇カーブを追加
* レベルに応じた背景変化（流れ星、ビル群、空の色合い変化）を追加
* ゲームオーバー時のフラッシュ演出を追加

## v0.3 背景演出

* ネオンカラーの星空・流れ星演出を追加
* 月・地平線・シンセウェイブ風パースグリッドを追加
* ジャンプ中の軌跡（トレイル）・ジャンプパーティクルを追加

## v0.2 ネオンテーマ

* ネオンカラーパレット（シアン・マゼンタ・パープル）と発光表現（shadowBlur）を導入
* Google Fonts「Orbitron」を導入しUIフォントを統一
* ハイスコアの `localStorage` 保存を追加
* 障害物のランダム生成・スコアに応じた速度上昇を追加

## v0.1 基本ゲームループ

* Canvas のセットアップ、地面・プレイヤーの描画を追加
* 重力・ジャンプ操作（スペースキー）を追加
* 障害物の移動と衝突判定を追加
* スコア加算と基本的なゲームループ（`update` → `render`）を追加
